<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Teacher Dashboard</title>
</head>
<body>
    <h1>Welcome to the Teacher Dashboard</h1>
    
    <!-- Button to add result -->
    <button><a href="/teacher/addresult">
        Add Result </a></button>
    
    <!-- Display added results -->
    <h2>Added Results:</h2>
<<<<<<< HEAD

        <table>
            <thead>
                <tr>
                    <th>Roll Number</th>
                    <th>Date of Birth</th>
                    <th>Name</th>
                    <th>Total Score</th>
                </tr>
            </thead>
            <tbody>
                <% results.forEach(function(result) { %>
                    <tr>
                        <td><%= result.rollNumber %></td>
                        <td><%= result.dob %></td>
                        <td><%= result.name %></td>
                        <td><%= result.totalScore %></td>
                    </tr>
                <% }) %>
            </tbody> 
        </table>
    

=======
    
    <% if (results && results.length > 0) { %>
        <ul>
            <% results.forEach(function(result) { %>
                <li>Subject: <%= result.subject %>, Marks: <%= result.marks %></li>
            <% }); %>
        </ul>
    <% } else { %>
        <p>No marks found yet.</p>
    <% } %>
>>>>>>> 680eed3 (dashboard template corrected)
</body>
</html>
